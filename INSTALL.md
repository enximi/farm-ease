# 安装农场随心合集

合集包含四个独立 Mod：随心往返（TravelEase）、田园巧整（GardenEase）、从容钓鱼（FishingEase）、随取随用（StorageEase）。
可以全装，也可以只装任意组合。无需额外的核心 Mod。

## 全新安装

退出游戏，把需要的 Mod 文件夹放入游戏的 `Mods` 目录，通过 SMAPI 启动。
进入存档后，原生菜单最右侧的芽苗标签就是「农场随心」。L3 / F7 直达，LT / RT 切换标签。

## 多人安装

- 传送、一键睡觉和钓鱼：想使用的玩家各自在自己的电脑安装相应 Mod，不要求房主或其他玩家安装。随心往返当前为 1.3.0；一键睡觉沿用原版多人等待机制。
- 农场整理：房主和想整理的玩家安装相同版本的 GardenEase；当前为 1.6.0。不参与整理的玩家可以不安装。
- 远程仓储：房主和使用者安装相同版本 StorageEase，当前为 1.1.0。默认自己的箱子可远程打开和用于制作，其他玩家需箱主主动共享。
- 本地分屏：本机安装一份即可，操作及传送返回记录按屏幕区分；钓鱼当前设置分开，本机配置文件作为共同的下次启动默认值。
- 建议同一台电脑上系列 Mod 一起升级，确保共用菜单来自最新版本。多人整理不暂停时间，支持同时整理不同格子；选中的格子由本人暂时保留，撤销记录按玩家分开。
- 本版已完成多人代码适配与编译，尚未启动游戏验证联机、分屏及断线恢复。

## 从旧 FarmEase 升级

旧 FarmEase 同时含传送与整理。升级时必须先把旧 `Mods/FarmEase` 文件夹移到 Mods 目录之外保留，避免旧功能和菜单重复加载。
不要移走 `SaveBackup`、`ConsoleCommands` 或其他无关 Mod。

推荐从源码工作区运行 `./build.sh --install`，会自动备份旧版、迁移配置、安装四个独立 Mod，并保留现有钓鱼参数。
旧版 `config.json` 中的回家按钮和长按时间迁到 `Mods/TravelEase/config.json`。
菜单按钮、重复等待/间隔、面板宽度迁到 `Mods/.farm-ease-menu.json`。这份共享设置不随某个功能的卸载消失。
正式存档不需要转换。

手动安装时，可自行将上述对应字段复制到新配置；不迁移则采用默认按键与速度。
已有 FishingEase 的 `config.json` 应保留，安装包不会附带覆盖用户设置的配置文件。

## 停用

关闭游戏后，把某个 Mod 文件夹移出 Mods，即可独立停用。再次启动时，统一菜单只显示仍然安装的功能。
四个全都移走后，菜单入口也消失。
田园巧整已经保存到存档中的作物布局会保留，卸载不会自动撤销搬移。

随取随用停用后，箱子物品仍在原箱子内；名称与开关等元数据保留，不需要库存迁移。
